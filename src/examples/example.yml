description: >
  Retrieve and store SSM Parameters as .env file
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1
  orbs:
    tryzero-orb: nguyenkhavi/tryzero-orb@1.2.3
  workflows:
    use-my-orb:
      jobs:
      - tryzero-orb/greet
